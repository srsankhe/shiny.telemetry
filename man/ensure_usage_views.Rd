% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/usage-views.R
\name{ensure_usage_views}
\alias{ensure_usage_views}
\title{Install/update the usage-db answer-layer views}
\usage{
ensure_usage_views(data_storage)
}
\arguments{
\item{data_storage}{a \code{\link{DataStorageSQLFamily}} instance (e.g.
\code{\link{DataStoragePostgreSQL}}) connected to the usage database.}
}
\value{
The number of SQL statements applied, invisibly.
}
\description{
Applies \code{inst/sql/usage-db.schema.sql} (indexes, JSON accessor functions,
and the \verb{v_*} reporting views the product-usage dashboard queries) against
the connection held by \code{data_storage}. The script is idempotent: it uses
\verb{CREATE INDEX IF NOT EXISTS} and \verb{CREATE OR REPLACE FUNCTION}/\code{VIEW}, so
re-running it (e.g. after \code{event_log}'s JSON shape gains a new field) is
expected and safe.

Postgres-only. The connecting role must be allowed to create functions and
views in the \code{public} schema. This is meant to be run once, by hand, from
the analytics dashboard process (or an admin session) against a
\code{DataStorageSQLFamily}-backed store -- never from the instrumented app
itself, which only ever needs to \code{insert()}.
}
